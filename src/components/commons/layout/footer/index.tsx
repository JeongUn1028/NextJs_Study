import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 50px;
  background-color: yellow;
`;

export default function LayoutFooter(): JSX.Element {
  return <Wrapper>LayoutFooter</Wrapper>;
}
